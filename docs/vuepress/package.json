{
  "scripts": {
    "docs:dev": "vuepress dev docs --openssl-legacy-provide",
    "docs:build": "SET NODE_OPTIONS=--openssl-legacy-provider && vuepress build docs"
  },
  "devDependencies": {
    "vuepress": "^1.9.7"
  }
}
